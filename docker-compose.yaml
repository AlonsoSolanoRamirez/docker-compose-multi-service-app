version: "3.8"

services:
  backend:
    build: ./backend
    container_name: backend_container
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: frontend_container
    ports:
      - "8080:80" 
